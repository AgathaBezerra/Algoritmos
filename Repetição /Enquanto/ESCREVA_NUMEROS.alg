Algoritmo "escreva_numeros"

// Escreva um algoritmo que leia um número inteiro como entrada.
//Então, calcule e escreva a tabuada do número informado.

Var

    num: inteiro
    
Inicio

      num <- 1
      
      enquanto (num >= 0) e (num <= 10) faca
               escreva(num)
               num <- num * 1
               leia(num)
      fimenquanto



Fimalgoritmo